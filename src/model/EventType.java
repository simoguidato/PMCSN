package model;

public enum EventType {
    ARRIVAL,       // Arrivo di un nuovo utente dall'esterno
    DEPARTURE_A,   // Fine di un servizio al Server A
    DEPARTURE_B,   // Fine di un servizio al Server B
    DEPARTURE_P
}
